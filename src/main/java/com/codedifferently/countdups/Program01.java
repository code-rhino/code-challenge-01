package com.codedifferently.countdups;

public class Program01 {

    public String countUniqueWords(String input){
        String[] newWord= input.split("");
        // cycle through the string for spaces (to identify new words)

        // compare the word lengths

        // compare the chars or each word
        for(int i=0; i< input.length(); i++){
            if(String[] =input.split("")){

            }
        }
        return null;
    }
}
