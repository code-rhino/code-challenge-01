package com.codedifferently.countdups;

public class Program01 {

    public String countUniqueWords(String input){
        return null;
    }
    String count = "that that is is that that is not is not is that it it is wutang";
    //given the string
    //count the unique words
    for(int i = 0; i < count.length; i++){
        if (count =
    }

    //how many times do they occur

    {
        System.out.println(count.contains("that"));
    }
}
